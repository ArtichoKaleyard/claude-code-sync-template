# 默认：所有文本文件规范化存储为 LF
* text=auto eol=lf

# Shell 脚本：强制 LF（Unix 原生）
*.sh text eol=lf

# PowerShell 脚本：强制 CRLF（Windows 原生）
*.ps1 text eol=crlf

# 配置文件：强制 LF
*.conf text eol=lf
*.json text eol=lf
*.md   text eol=lf

# 二进制文件：不做行尾转换
*.png  binary
*.jpg  binary
*.gif  binary
*.ico  binary
*.zip  binary
